[build]
  command = "npm run build"
  publish = "dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ."

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "REPO_NAME,REPO_OWNER,VITE_TENANT_ID"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200